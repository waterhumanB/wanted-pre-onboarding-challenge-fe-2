/** @function CreateTodo
 * @param {string} id todo의 이름을 추가한다.
 * @param {string} context todo 한 개의 내용 - 내용이 없으면 todo를 추가 할 수 없다.
 * @param {string} category todo 마다 종류 ex) 아침, 점심, 저녁, 회사, 공부 등
 * @param {string[]} tags todo 태그들? 카테고리와 차이를 모르겠음
 * @param {boolean} isChecked todo 완료후 체크한 값
 * @returns {object} {id:string, context:string, category:string, targs: string[], isChecked: boolean}
 */
const CreateTodo = () => {};
