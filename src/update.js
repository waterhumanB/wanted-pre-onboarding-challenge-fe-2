/** @function UpdateTodo
 * ID를 제외한 모든 속성을 수정할 수 있다.
 * @param {string} id 수정할 id 값을 파라미터로 넣는다.
 * @returns {boolean} 수정이 완료 되면 true 아니면 false
 */
const UpdateTodo = () => {};

/** @function UpdateTodoTag
 * 특정 할 일의 특정 태그를 수정할 수 있다.
 * @param {string} id 수정할 id 값을 파라미터로 넣는다.
 * @returns {boolean} 수정이 완료 되면 true 아니면 false
 */
const UpdateTodoTag = () => {};
