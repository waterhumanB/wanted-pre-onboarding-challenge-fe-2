/** @function ReadTodoAll
 * 모든 할 일을 조회할 수 있다.
 * 배열 형태로 받는다.
 * @returns {object[]} 모든 todo 값을 배열 형태로 받는다.
 */
const ReadTodoAll = () => {};

/** @function ReadTodoId
 * ID를 기반으로 특정 할 일을 조회할 수 있다.
 * @param {string} id 조회할 id 값을 파라미터로 넣는다.
 * @returns {object} {id:string, context:string, tags:string[], isChecked: boolean}
 */
const ReadTodoId = () => {};
