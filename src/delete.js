/** @function DeleteTodo
 * ID를 기반으로 특정 할 일을 삭제할 수 있다.
 * @param {string} id 삭제할 id 값을 파라미터로 넣는다.
 * @returns {boolean} 해당 todo가 삭제 되면 true 아니면 false
 */
const DeleteTodo = () => {};

/** @function DeleteTodoAll
 * 모든 할 일을 제거할 수 있다.
 * @returns {boolean} 모든 todo가 삭제되면 true 아니면 false
 */
const DeleteTodoAll = () => {};

/** @function DeleteTodoTag
 * 특정 할 일의 특정 태그를 삭제할 수 있다.
 * @param {string} id 태그를 삭제할 id 값을 파라미터로 넣는다.
 * @returns {boolean} 테그가 삭제 되면 true 아니면 false
 */
const DeleteTodoTag = () => {};

/** @function DeleteTodoTagAll
 * 특정 할 일의 모든 태그를 제거할 수 있다.
 * @param {string} id 모든 태그를 삭제할 id 값을 파라미터로 넣는다.
 * @returns {boolean} 모든 태그가 삭제되면 ture 아니면 false
 */
const DeleteTodoTagAll = () => {};
